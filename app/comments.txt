git init
git add .
git commit -m 'first'
gst

THEN GITHUB STUFF
git remote add origin https://github.com/Jitgitbit/kotlin_exercise_one.git
git push -u origin master

THEN KEEP IT SYNC
git add .
git commit -m 'second'
gst
git push origin
git add .
git commit -m 'third'
gst
git push origin
gst
AND SO ON...

LET'S ROLL
git add .
git commit -m 'exploring with kotlin syntax, (pretty standard)'
gst
git push origin
gst
